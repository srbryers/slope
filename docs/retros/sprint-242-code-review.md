# Sprint 242 — Code Implementation Correctness Review

- **Reviewer:** code-implementation-correctness-reviewer (independent correctness lane)
- **Branch:** `feat/S242-closeout-integrity` @ `b090c16a641703b53b3173cf1ef61a9a97975561` (initial review) / `50a731769fa97c639eba731d6c65baf63e85de3c` (delta re-review — see addendum at the bottom)
- **FINAL VERDICT: Approve with notes** — all four blocking findings (F1–F4) verified fixed; one narrow new regression (R1, `--stdout`/`--output` gate reopen) is a required pre-release follow-up. See the addendum.
- **Scope:** `git diff main...feat/S242-closeout-integrity -- src/ tests/` (review.ts overwrite guard + gate validation + repairMojibake, formatter `--` placeholders, sprint statusCommand closed-out state)
- **Tests:** `tests/cli/review-command.test.ts`, `tests/core/formatter.test.ts`, `tests/cli/commands/sprint-gate.test.ts` — 72/72 pass at HEAD.
- **Architect-review fixes verified:** repairMojibake moved to the source (stdout now matches disk — this closes the no-op/idempotence hazard I probed against the earlier revision), `reviewRequiredSections` shape validation (non-array ignored with warning, non-string entries filtered), refusal message wording.

## Verdict

**Changes requested.** S242-2 (formatter) and S242-3 (statusCommand) are correct. S242-1's overwrite protection has two real silent-data-loss bugs (F1, F2) in exactly the scenario #616 exists to protect, plus a gate-integrity gap (F4). None are caught by the current tests because the tests only exercise never-generated vs. pristine-generated files.

## Findings

### F1 — MAJOR — Enriched generated reviews are silently clobbered (marker persists through human edits)

`src/cli/commands/review.ts:124` — the refusal fires only when `!existing.includes(REVIEW_GENERATED_MARKER)`. Once slope has generated a review, the marker stays in the file forever, so **any subsequent human/agent enrichment of that file is unprotected**: the next `slope review` run overwrites it silently (exit 0, no warning), confirmed by the code path and by the test "Marker-carrying file regenerates freely on a later run".

This defeats the feature's own companion mechanism. The intended `reviewRequiredSections` workflow is: gate demands `## What went wrong` → agent appends it to the generated review (marker still on line 1) → gate closes. The documented post-hole flow then runs `slope review findings add` → `slope review amend` → re-run `slope review` to regenerate — which **wipes the appended sections**. The gate check then reports them missing (and per F4 the gate may silently stay closed anyway). Recovery only via git.

- **Failing input:** file = `<!-- generated by slope review -->\n\n## Sprint 130 Review…\n## What went wrong\n<human text>\n`; run `slope review` → `## What went wrong` destroyed, no refusal.
- **Required fix:** protect *modified* content, not *unmarked* content. Embed a content hash in the marker (e.g. `<!-- generated by slope review sha256:<hash-of-body> -->`); overwrite freely only when the hash matches the file body, otherwise refuse without `--force`. This also fixes F2.

### F2 — MAJOR — Project-authored files that merely quote the marker are misclassified as generated

`src/cli/commands/review.ts:124` — `includes()` matches the marker anywhere. A hand-authored review that quotes the marker string — a retro discussing this very feature, a doc showing slope output, a code fence — is treated as slope-generated and **silently overwritten** without `--force`. This repo dogfoods slope and writes retros about slope features, so it is the most likely victim.

- **Failing input:** authored `sprint-N-review.md` containing the sentence: "slope now writes `<!-- generated by slope review -->` at the top of generated files." → next `slope review` run replaces the whole file, exit 0.
- **Required fix:** at minimum anchor the check (`existing.startsWith(REVIEW_GENERATED_MARKER)`); the F1 hash-marker approach subsumes this.

### F3 — MEDIUM — repairMojibake is asymmetric: right double quotes and ellipses stay mangled

`src/cli/commands/review.ts:72-79` — the table repairs left double quote (`â€œ`, cp1252 0x9C) but not right double quote: U+201D is UTF-8 `E2 80 9D`, and 0x9D is unmapped in cp1252 (decodes to control U+009D), so the mojibake is `â€` and no pattern matches it. Same for ellipsis `â€¦` (U+2026 → `E2 80 A6`, 0xA6 = `¦`) and bullet `â€¢`.

Probe (cp1252-decode of UTF-8, the exact #616 failure mode):

```text
original: He said “done” — it’s fine… 5<NBSP>km
mangled : He said â€œdoneâ€<9D> â€” itâ€™s fineâ€¦ 5Â<A0>km
repaired: He said “doneâ€<9D> — it’s fineâ€¦ 5 km   ← unbalanced quotes, â€ residue
```

Any scorecard note containing a closing curly quote or ellipsis produces a review that still contains `â€` — the exact residue the new test asserts against (`expect(written).not.toContain('â€')` passes only because the fixture omits these characters).

- **Fix:** add `[/â€/g, '”']`, `[/â€¦/g, '…']`, `[/â€¢/g, '•']`, `[/Â°/g, '°']` (or do a decode-based round-trip repair), and extend the test fixture with `”` and `…`.
- **Note:** latin1/ISO-8859-1-decoded mojibake (controls instead of cp1252 punctuation) is not covered at all; fine if #616 is strictly cp1252, worth a comment.

### F4 — MEDIUM — review_md gate is never re-opened when the on-disk review becomes non-compliant

`src/cli/commands/review.ts:158-163` — the missing-sections branch only prints "gate left open"; `updateGate(cwd, 'review_md', false)` is never called. If the gate was already `true` from an earlier compliant run and the review is then regenerated without the sections (`--force`, or the F1 wipe), the gate silently stays closed and closeout proceeds against a non-compliant review — the message even lies ("left open") when the gate is in fact closed.

- **Failing sequence:** compliant review on disk → `slope review` closes gate → `slope review --force` (or amend/regenerate) replaces it with a generated review lacking the sections → stderr says "gate left open" but `gates.review_md` remains `true`.
- **Fix:** in the missing branch, explicitly `updateGate(cwd, 'review_md', false)` (and adjust the message), or at least detect and warn that the gate was previously closed.

### F5 — LOW/MEDIUM — `--stdout` closes the gate against content that is not on disk

`src/cli/commands/review.ts:113-114` — `reviewOnDisk` is initialized to the freshly generated review and, on `--stdout` (outputPath === null), the write block is skipped entirely, so the gate validates content that exists nowhere on disk. The comment "whatever ends up on disk" is false for this path. Consequences:

1. With no `reviewRequiredSections`, `slope review --stdout` sets `review_md = true` with **no review file anywhere**. This is behavior-parity with main (main called `updateGate` unconditionally), so not a regression — but the sprint is titled "closeout integrity" and the new code now codifies the fiction.
2. With sections configured and a *compliant authored file already on disk*, `--stdout` validates the generated (section-less) content instead and leaves the gate open despite a compliant review existing. Wrong direction is conservative, but inconsistent with the refusal path, which validates the disk file.

- **Fix:** on stdout-only runs, either skip the gate update, or read and validate the default-path file if it exists.

### F6 — LOW — repairMojibake rewrites content that intentionally quotes mojibake

Repair now runs at the source on the whole review, so a scorecard note that *quotes* mojibake (e.g. an S242 shot note: "fixed the `â€”` artifacts in reviews") is silently rewritten to "fixed the `—` artifacts" in both stdout and the file — the quoted evidence no longer says what the author wrote. This repo's own #616/S242 retro notes are the most likely input. The stderr note ("repaired cp1252-mangled character sequences") is the only signal. Acceptable tradeoff, but document it; there is no opt-out flag.

### F7 — LOW — required-sections match is an unanchored substring over the whole document

`src/cli/commands/review.ts:159-160` — `haystack.includes(section.toLowerCase())` matches prose or code fences, not just headings. A review containing the sentence "the ## What went wrong section is still TODO" (or a fenced example) closes the gate without the heading existing. Suggest a per-line anchored match (`/^\s{0,3}#{1,6}\s*.../` against each line) since the config documents these as "section headings".

### F8 — INFO — Migration behavior change: all pre-S242 generated reviews now refuse to regenerate

Reviews generated before this release carry no marker, so they are indistinguishable from project-authored files: the first `slope review` re-run in every existing project refuses and requires `--force`. Probably the right conservative default, but it needs a changelog note. Also note the refusal exits 0 (intentional, since the gate is still honored) — automation checking only exit codes will not notice that content was not refreshed.

## Clean areas

- **S242-3 statusCommand** (`src/cli/commands/sprint.ts:1010-1061`) — correct. `closedOut` branch is only reachable in the all-gates-complete case for the "Next:" line; `phase === 'complete'` with open gates prints the same derived status string main already printed (`complete`); no code in `src/` parses `ready_for_pr`; resume/rollover/gate paths (`sprint.ts:1223,1227`, `gate.ts:90`, `sprint-state.ts:388 hasActiveSprint`) already special-case `phase === 'complete'` and are unaffected. Both new tests are meaningful.
- **S242-2 formatter `--` placeholders** (`src/core/formatter.ts`) — trivially correct; as a bonus it removes the em dash that was itself the likely #616 mojibake vector in review tables.
- **`--force` plumbing** (`src/cli/index.ts:139`) — correct; no interaction with review-state routing or positional path parsing.
- **repairMojibake idempotence** — verified by probe; replacements cannot re-form any pattern, so regenerate-compare (`existing === nextContent`) is stable across runs.
- **Config shape validation** — non-array `reviewRequiredSections` warned and ignored; non-string/empty entries filtered. Closeout cannot crash on bad config.

## Required fixes (blocking)

1. **F1/F2:** replace bare `includes(marker)` with a tamper-detecting marker (content hash) or, minimally, `startsWith` + refuse when a marker-carrying file's body differs from the last generated output. Add tests: (a) generated file + appended section is not clobbered without `--force`; (b) authored file quoting the marker is not clobbered.
2. **F4:** re-open (`updateGate(..., false)`) the review_md gate when required sections are missing, and test the previously-closed-gate sequence.
3. **F3:** complete the mojibake table (`”`, `…`, `•`, `°`) and extend the fixture so `not.toContain('â€')` actually bites.

F5–F7 are non-blocking hardening; F8 needs a changelog entry.

---

# Addendum — Delta Re-Review @ `50a7317`

Scope: `fix(S242): address code review findings — content-hash marker, gate reopen, repair table` (`b090c16..50a7317`, `src/cli/commands/review.ts` + `tests/cli/review-command.test.ts` only; formatter and sprint.ts untouched). All three targeted suites pass: 75/75.

## Blocking findings from the initial review — verified fixed

### F1/F2 — FIXED — content-hash marker (`isPristineGeneratedReview`)

`src/cli/commands/review.ts:76-95` — the marker is now a full first line `<!-- generated by slope review; content-hash: <sha256-16> -->`, matched by an anchored regex on line 1 only, with the hash covering the entire remainder of the file (including the leading newline, consistent between `renderGeneratedReview` and the checker). Probed directly:

| Input | `isPristineGeneratedReview` | Outcome |
|---|---|---|
| freshly generated file | `true` | regenerates freely |
| generated file + appended `## What went wrong` | `false` | refused without `--force` (F1 closed) |
| authored file quoting the marker line mid-document | `false` | refused (F2 closed) |
| marker line with no trailing newline | `false` | refused (safe) |
| generated file after CRLF checkout (git autocrlf) | `false` | refused (conservative — see note N1) |

Both new tests (`#616 F1`, `#616 F2`) assert the on-disk bytes survive, not just the stderr message. Correct.

### F4 — FIXED (with one side effect, see R1) — gate reopen

`src/cli/commands/review.ts:195-198` — missing required sections now call `updateGate(cwd, 'review_md', false)` and the message says "reopened". The new test covers the exact sequence I demanded (gate `true` → `--force` regeneration without sections → gate `false`). Correct for the write path. The reopen semantics leak into view-only runs — that is R1 below.

### F3 — FIXED — repair table completed for the reported characters

`src/cli/commands/review.ts:104-105` — the new right-double-quote pattern (`â€` followed by raw U+009D or U+FFFD) handles the cp1252-unmapped 0x9D both raw and after a lossy replacement-character round-trip, plus the `â€¦` → `…` ellipsis entry. The fixture now contains a raw U+009D byte (verified by a codepoint dump of the test source), so `not.toContain('â€')` finally has teeth. My original probe input (`“done” — it’s fine…`) now repairs fully. Residual gaps carried as note N2.

### F7 (bonus) — FIXED — line-anchored section matching

`src/cli/commands/review.ts:189-192` — required headings now match via trimmed-line `startsWith`, so prose that mentions a heading mid-sentence no longer satisfies the gate. Residual: a line inside a fenced code block that *is* the heading still matches (minor, acceptable).

## New finding introduced by the delta

### R1 — MEDIUM (required pre-release follow-up) — view-only runs now actively reopen a closed gate

The F4 fix executes on **every** run where `sprintState.sprint === card.sprint_number`, but `reviewOnDisk` is only the real on-disk content on the canonical-path branch. On `--stdout` (`outputPath === null`) it is the freshly generated review, and on `--output=<other>` it is that other file — neither contains project-authored sections. Result, reproduced by probe against `50a7317`:

- Setup: compliant authored review at the canonical path, gate `review_md = true`, `reviewRequiredSections` configured.
- `slope review --stdout` (pure view; disk untouched) → **gate flips to `false`**.
- `slope review --output=scratch-copy.md` → same flip.

Before this delta the same runs merely printed "left open" without mutating state; now a read-only invocation destroys a legitimately closed gate. It self-heals on the next default-path run (the refusal branch re-validates the compliant file and re-closes the gate), and it only fires when the opt-in `reviewRequiredSections` is set — which is why this is a follow-up rather than a re-block. But sections-configured projects are exactly the population that views reviews pre-PR, so the gate will flap in practice.

- **Failing input:** the two probe scenarios above (scratchpad `stdout-gate.probe.ts`; observed `gates.review_md === false` after both variants).
- **Required fix:** perform the gate update (close *or* reopen) only when validating the canonical on-disk review — i.e. skip the gate block entirely when the run targets `--stdout` or an explicit `--output` path, or read the canonical-path file for gate purposes regardless of output target. This also retires the initial review's F5 for the reopen direction.

## Notes (non-blocking, unchanged or new)

- **N1 (new):** CRLF-normalized checkouts of generated reviews fail the pristine hash and require `--force` once. Conservative direction; worth one line in the docs if Windows users report it.
- **N2 (F3 residual):** bullet `â€¢`, degree `Â°`, and latin1/ISO-8859-1-decoded variants remain unrepaired; the table is now adequate for the #616 corpus.
- **N3 (was F5):** `--stdout` with **no** sections configured still closes the gate with nothing on disk (main parity). Fixing R1 as suggested also resolves this.
- **N4 (was F6):** repair still rewrites content that intentionally quotes mojibake; the stderr note is the only signal. Documented tradeoff.
- **N5 (was F8):** pre-S242 generated reviews (no marker) refuse regeneration once and need `--force`; needs a changelog entry.

## Final verdict

**Approve with notes.** F1–F4 and F7 are correctly fixed, with meaningful tests that assert on-disk bytes and gate state rather than messages. The one defect introduced by the delta (R1) is confined to the opt-in `reviewRequiredSections` feature interacting with view-only flags, is recoverable, and has a small, well-defined fix — land it before the release that ships `reviewRequiredSections`, alongside the N5 changelog note.

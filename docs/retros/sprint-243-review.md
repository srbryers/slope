<!-- generated by slope review; content-hash: 242ef58020afe70a -->
## Sprint 243 Review: Stale State and Guard Scoping

### SLOPE Scorecard Summary

| Metric | Value |
|---|---|
| Par | 4 |
| Slope | 4 |
| Score | 7 |
| Label | Triple+ |
| Fairway % | 100% (3/3) |
| GIR % | 100% (3/3) |
| Putts | 0 |
| Penalties | 0 |
| Hazard Penalties | 3 |

### Shot-by-Shot (Tickets Delivered: 3)

| Ticket | Club | Result | Hazards | Notes |
|---|---|---|---|---|
| S243-1 | Driver | Green | Bunker: [architect review] Dead-session evidence could pause a live teammate's execution after a 10-minute heartbeat lapse (peer cleanStaleSessions deletes the row); fixed with a 60-minute execution-quiet grace window.; Rough: [code review] Dead-session staleness was dead code in production: no shipped path set session_id on executions. slope sprint run and the loop adapter now bind executions to SLOPE_SESSION_ID.; Rough: [code review] slope status could advertise a live parallel execution as dangling with a cleanup command that would pause it; surfacing now excludes newer-running evidence and passes currentSessionId. | findStaleWorkflowExecutions now counts a dead owning session as staleness evidence — gated on session tracking being in use and never for the invoking session — and slope status lists dangling executions with the cleanup command. |
| S243-2 | Long Iron | Green | Bunker: [architect review] isWithinRepo lacked realpath handling (symlinked roots could bypass the gate) and cast file_path unchecked; advisory recommended unvalidated phase complete. All fixed. | workflow-step-gate allows any Edit/Write whose target resolves outside cwd before touching the store. Main had already narrowed phase-boundary to slope commands; a gh issue create regression test locks that in. |
| S243-3 | Short Iron | Green | Bunker: [architect review] Raw sprintOrderValue mis-decoded modern sprint ids ending in 5 (245 read as 24.5), making the staleness downgrade both fail-open and fail-closed; fixed with roadmap-aware ordering and roadmap-membership evidence. | When scorecards exist at or past the target phase's first sprint, phase-boundary emits a non-blocking advisory recommending slope phase complete N instead of denying. |

### Conditions

| Condition | Impact | Description |
|---|---|---|
| undefined | undefined | undefined |

### Hazards Discovered

| Type | Ticket | Description |
|---|---|---|
| Bunker | S243-1 | [architect review] Dead-session evidence could pause a live teammate's execution after a 10-minute heartbeat lapse (peer cleanStaleSessions deletes the row); fixed with a 60-minute execution-quiet grace window. |
| Rough | S243-1 | [code review] Dead-session staleness was dead code in production: no shipped path set session_id on executions. slope sprint run and the loop adapter now bind executions to SLOPE_SESSION_ID. |
| Rough | S243-1 | [code review] slope status could advertise a live parallel execution as dangling with a cleanup command that would pause it; surfacing now excludes newer-running evidence and passes currentSessionId. |
| Bunker | S243-2 | [architect review] isWithinRepo lacked realpath handling (symlinked roots could bypass the gate) and cast file_path unchecked; advisory recommended unvalidated phase complete. All fixed. |
| Bunker | S243-3 | [architect review] Raw sprintOrderValue mis-decoded modern sprint ids ending in 5 (245 read as 24.5), making the staleness downgrade both fail-open and fail-closed; fixed with roadmap-aware ordering and roadmap-membership evidence. |

**Known hazards for future sprints:**
- Dead-session staleness can misfire in multi-agent setups if guarded only by session-row presence; it is fenced to tracked-session repos and never the invoking session, pending review scrutiny.

### Course Management Notes

- Issue #621 was filed from an older slope version; each ticket was re-scoped against current main before implementation.


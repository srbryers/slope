<!-- generated by slope review; content-hash: eb66386e1466c870 -->
## Sprint 267.6 Review: Ticket Completion Truth

### SLOPE Scorecard Summary

| Metric | Value |
|---|---|
| Par | 5 |
| Slope | 4 |
| Score | 16 |
| Label | Triple+ |
| Fairway % | 60% (3/5) |
| GIR % | 60% (3/5) |
| Putts | 0 |
| Penalties | 0 |
| Hazard Penalties | 11 |

### Shot-by-Shot (Tickets Delivered: 5)

| Ticket | Club | Result | Hazards | Notes |
|---|---|---|---|---|
| S267.6-1 | Long Iron | In the Hole | -- | The answer was no new table. Checked rather than assumed: idx_events_ticket exists in both backends, and events.sprint_number was migrated to TEXT in both, so it stores "267.10" without collapsing it to 267.1. Both re-reviews verified those claims independently. What was actually missing was a single reader, not a table. Building one would have made the most expensive ticket in three phases an unnecessary one and forced three tickets into serial execution behind it. |
| S267.6-2 | Short Iron | Green | Rough: [code review] Shipped with no test. Reverting the hardening back to a swallowing catch left all 15 tests green, so the fix was unverified. Covered now by dropping the events table and asserting exit 1 with the claim still held.; Bunker: [code review] The read side kept the exact failure mode the write side had just lost: a failed ledger query became an empty map, so every surface reported the ticket unfinished with no diagnostic. Fixing one half of a swallow and leaving the other is worse than fixing neither, because the write now looks trustworthy.; Bunker: [code re-review] The read fix covered the query layer only. agent status still swallowed a store-open failure, and that is the surface agents consume: store.list runs before the ledger read, so a failure there skipped the read entirely and landed in the outer catch with no error set. | The write now exits 1 and keeps the claim, so a lost write cannot report success. Scored green rather than better because the read half of the same swallow survived two passes: first at the query layer, then at the store-open layer in the one surface most likely to be automated against. |
| S267.6-3 | Long Iron | Missed Short | Water: [architect review] Half delivered. The three surfaces converged on completions and kept three different claim policies: now skipped every claimed ticket, agent status preferred a claimed one and matched claims from ANY player, and compact roadmap status ignored claims entirely. With one claim open, now said start S1-2 while roadmap status said work S1-1. In a two-agent sprint, agent status pointed both agents at the same ticket.; Water: [architect review] The test written for this ticket compared agent and now only after ticket done had released the claim, which is the single state where the two policies coincide. A test aimed at the exact scenario the ticket names, constructed so it could not fail.; Bunker: [code re-review] selectNextTicket decides "your own claim" by player name, and no read surface accepted --actor while every write surface did. So a claim made under --actor=agent-a could never be matched back, and now advised force-taking work the caller already held.; Rough: [code re-review] now --json reported tickets.completed as 0 while ledgerError was set. The text renderer already printed "unknown"; only the JSON path stated zero as fact, which is what an automated consumer reads.; Rough: [self] Printing the Start hint for in-flight work meant dropping the gate that suppressed it, and that gate was load-bearing: a pending review decision outranks the ticket. Caught by an existing now test, not by me.; Rough: [architect review] now --json omitted nextTicket entirely when the sprint was exhausted while agent status used null, so the two surfaces the ticket exists to align answered "nothing next" in two different shapes. | Scored a miss, and the worst of the sprint. The ticket says three surfaces agree; the first cut delivered three surfaces that agreed on completions and disagreed on claims, with a test narrow enough to miss it. selectNextTicket is now the one rule all three call, with a reason distinguishing all_claimed from all_complete. Honest residual limitation, filed as #715 and documented rather than implied away: identity is player name, claims never record session_id, so two agents sharing a machine identity still collide. --actor on all three read surfaces is the workaround, not a fix. |
| S267.6-4 | Short Iron | Green | Bunker: [both reviews] A third path still stored the value verbatim: outside a git work tree the explicit flag returned unresolved, so --commit=HEAD became permanent evidence, printed one line above a warning saying no SHA had been attached. The command contradicted itself and #698 stayed fully reproducible there. isInsideGitWorkTree is also false when git is absent from PATH, which widens the reach. | Explicit values rev-parse through ^{commit}, so an abbreviated sha expands and a tree sha is refused. The refusal happens before the claim is released. One path missed on the first cut, and it was the path with no test. |
| S267.6-5 | Short Iron | Missed Short | Water: [both reviews] repair and show resolved the CURRENT sprint through sprint state rather than the ticket's own sprint. Once state advanced, show reported a real completion as absent and repair refused it, advising ticket done, which needs a claim and would have filed the record under the wrong sprint. Evidence is audited after rollover, so the ticket's own scenario was the failing one. getEventsByTicket already existed and ticket_key is indexed in both backends.; Rough: [code review] repair rewrote player to whoever ran it, so correcting evidence silently reattributed the work.; Rough: [code review] ticket show --json emitted `completed` only in the negative case, so a consumer reading that field saw a finished ticket as undefined and an unfinished one as false. Both read as not done.; Rough: [code review] --notes= could not clear notes, because the guard tested truthiness rather than flag presence.; Rough: [code re-review] repaired_by was written into the event and read by nothing, so ticket show could not say who corrected the evidence. | Scored a miss. The ticket title says the repair path works after the claim is released, and it did, right up until sprint state moved on, which is when anyone would use it. Reading by sprint was the wrong primitive for a per-ticket command. slope ticket show was added alongside and is scope creep by the letter: not in any ticket, though repair genuinely needs a before view and nothing else surfaced recorded evidence. Registered and given a consistent JSON contract rather than split out. |

### Miss Pattern

| Direction | Count |
|---|---|
| Short (under-scoped) | 2 |

### Hazards Discovered

| Type | Ticket | Description |
|---|---|---|
| Rough | S267.6-2 | [code review] Shipped with no test. Reverting the hardening back to a swallowing catch left all 15 tests green, so the fix was unverified. Covered now by dropping the events table and asserting exit 1 with the claim still held. |
| Bunker | S267.6-2 | [code review] The read side kept the exact failure mode the write side had just lost: a failed ledger query became an empty map, so every surface reported the ticket unfinished with no diagnostic. Fixing one half of a swallow and leaving the other is worse than fixing neither, because the write now looks trustworthy. |
| Bunker | S267.6-2 | [code re-review] The read fix covered the query layer only. agent status still swallowed a store-open failure, and that is the surface agents consume: store.list runs before the ledger read, so a failure there skipped the read entirely and landed in the outer catch with no error set. |
| Water | S267.6-3 | [architect review] Half delivered. The three surfaces converged on completions and kept three different claim policies: now skipped every claimed ticket, agent status preferred a claimed one and matched claims from ANY player, and compact roadmap status ignored claims entirely. With one claim open, now said start S1-2 while roadmap status said work S1-1. In a two-agent sprint, agent status pointed both agents at the same ticket. |
| Water | S267.6-3 | [architect review] The test written for this ticket compared agent and now only after ticket done had released the claim, which is the single state where the two policies coincide. A test aimed at the exact scenario the ticket names, constructed so it could not fail. |
| Bunker | S267.6-3 | [code re-review] selectNextTicket decides "your own claim" by player name, and no read surface accepted --actor while every write surface did. So a claim made under --actor=agent-a could never be matched back, and now advised force-taking work the caller already held. |
| Rough | S267.6-3 | [code re-review] now --json reported tickets.completed as 0 while ledgerError was set. The text renderer already printed "unknown"; only the JSON path stated zero as fact, which is what an automated consumer reads. |
| Rough | S267.6-3 | [self] Printing the Start hint for in-flight work meant dropping the gate that suppressed it, and that gate was load-bearing: a pending review decision outranks the ticket. Caught by an existing now test, not by me. |
| Rough | S267.6-3 | [architect review] now --json omitted nextTicket entirely when the sprint was exhausted while agent status used null, so the two surfaces the ticket exists to align answered "nothing next" in two different shapes. |
| Bunker | S267.6-4 | [both reviews] A third path still stored the value verbatim: outside a git work tree the explicit flag returned unresolved, so --commit=HEAD became permanent evidence, printed one line above a warning saying no SHA had been attached. The command contradicted itself and #698 stayed fully reproducible there. isInsideGitWorkTree is also false when git is absent from PATH, which widens the reach. |
| Water | S267.6-5 | [both reviews] repair and show resolved the CURRENT sprint through sprint state rather than the ticket's own sprint. Once state advanced, show reported a real completion as absent and repair refused it, advising ticket done, which needs a claim and would have filed the record under the wrong sprint. Evidence is audited after rollover, so the ticket's own scenario was the failing one. getEventsByTicket already existed and ticket_key is indexed in both backends. |
| Rough | S267.6-5 | [code review] repair rewrote player to whoever ran it, so correcting evidence silently reattributed the work. |
| Rough | S267.6-5 | [code review] ticket show --json emitted `completed` only in the negative case, so a consumer reading that field saw a finished ticket as undefined and an unfinished one as false. Both read as not done. |
| Rough | S267.6-5 | [code review] --notes= could not clear notes, because the guard tested truthiness rather than flag presence. |
| Rough | S267.6-5 | [code re-review] repaired_by was written into the event and read by nothing, so ticket show could not say who corrected the evidence. |

**Known hazards for future sprints:**
- A per-ticket command must resolve by ticket key. Resolving through current sprint state works until state advances, which is exactly when evidence gets audited, so the failure lands in the one scenario the feature exists for.
- Converging surfaces on one input is not converging them on one answer. These three agreed on completions and kept three different claim policies, so the reported symptom survived under a live claim.
- A test that exercises the state where two policies coincide cannot detect that they differ. The claim was released before comparing, which is the single state where all three surfaces agree by accident.
- Fixing one side of a swallowed failure makes the other side worse, not neutral: the write now looks trustworthy while the read still turns 'could not look' into 'nothing recorded'.
- Read-only commands must not open a store. resolveStore runs mkdirSync plus a full schema migration on sqlite, so a status report materialises a database in a repo that never had one.
- Actor identity in SLOPE is player name; claims carry a session_id column that slope claim never populates. Any feature keying on 'my work' inherits that, and two agents on one machine collide silently (#715).


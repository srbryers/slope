# Sprint 81 Plan — The Universal Adapter (Harness Integrations)

**Par:** 4 (4 tickets)
**Slope:** 2
**Theme:** Codex CLI adapter, OpenCode plugin, cross-agent standardization

## Tickets

### T1: Codex CLI adapter
**Club:** short_iron
**Files:** `src/core/adapters/codex.ts`, `src/cli/commands/init.ts`

- Implement HarnessAdapter for Codex CLI
- Detection: `.codex/` directory or `config.toml`
- Hook config: `.codex/hooks.json` (same format as Claude Code)
- MCP config: `.codex/config.toml` [mcp] section
- AGENTS.md: already generated by `slope init`
- `slope init --codex` provider flag

### T2: OpenCode plugin
**Club:** short_iron
**Files:** `packages/opencode-plugin/src/index.ts`

- TypeScript plugin for OpenCode's hook system
- Register SLOPE tools via plugin API
- `tool.execute.before` for guard enforcement (file writes)
- `tool.execute.after` for post-action checks (push nudge)
- `session.created` for briefing injection
- Package as npm-installable plugin

### T3: Cross-agent path standardization
**Club:** wedge
**Files:** `src/cli/commands/init.ts`, `templates/`

- Ensure `slope init` generates `.agents/skills/` for ALL providers (not just Claude Code)
- Generate `AGENTS.md` for Codex compatibility
- Update `slope init --all` to include codex + opencode

### T4: Adapter test coverage
**Club:** wedge
**Files:** `tests/core/adapters/`

- Verify all adapters detect correctly
- Verify hook config generation for each platform
- Add Codex adapter tests
